# The Garden — Specification Index

**Project:** The Garden
**Version:** Living Specification (Index rebuilt 2026-07-09)
**Status:** Active

---

## Project Overview

The Garden is a deterministic, fully-autonomous living-world simulation. The player is an
observer ("gardener"), never an author — citizens are born, form relationships, build
settlements, found kingdoms, and generate history entirely through interacting rule systems.
Ten immutable "Constitutional Laws" (TG-001) govern every design decision (e.g. *Nothing
Happens Without Cause*, *History Is Permanent*, *The Simulation Owns Reality*).

This `specification/` library is the **authoritative** design reference for implementation.
It replaces the older, informal `blueprint/` folder (deleted 2026-07-09). One artifact of that
migration survives at `00_Master/The_Garden_Project_Blueprint.md` — an un-migrated,
un-versioned legacy overview kept for historical/orientation value only. **Where it conflicts
with any TG-### document, the TG-### document wins.**

Across all 84 documents in this library, one pattern holds without exception: these are
**design-philosophy and vocabulary specs, not algorithmic specs**. They define *what* a
system is, *why* it exists, its state-variable names, and its event catalog — but essentially
never a formula, numeric threshold, or data type. Any implementation task derived from this
library should budget explicit design work ("invent the missing numbers") before engineering
work ("write the code").

---

## Reading Order

```
01_Foundations         (TG-000 → TG-001)
        ↓
02_Engine              (TG-002 → TG-008)
        ↓
03_Sciences/01_Natural      (TG-100 → TG-180)
        ↓
03_Sciences/02_Life         (TG-200 → TG-290)
        ↓
03_Sciences/03_Cognitive    (TG-300 → TG-390)
        ↓
03_Sciences/04_Social       (TG-500 → TG-590)
        ↓
03_Sciences/05_Civilization (TG-600 → TG-690)
        ↓
04_Story               (TG-STRY-000 → TG-STRY-090)
        ↓
05_Observatory         (TG-OBS-000 → TG-OBS-009)
        ↓
06_Development         (TG-DEV-000 → TG-DEV-009)
```

Reality is defined before life. Life before intelligence. Intelligence before society.
Society before civilization. Civilization before story. Story before observation.
Observation before implementation. Whenever two documents conflict, **the earlier layer in
this order takes precedence.**

> The previous version of this index described a `01_World / 02_Sciences / 03_Cognitive /
> 04_Life / 05_Story / 06_Observatory / 07_Development` structure that does not match any
> folder that exists on disk. That version has been fully superseded by the structure below.

---

## Folder Structure

```
specification/
│
├── 00_Master/
│   └── The_Garden_Project_Blueprint.md      — legacy overview, no TG-ID, superseded by TG-### docs
│
├── 01_Foundations/                          — Volume I
│   ├── TG-000_Vision.md
│   └── TG-001_Constitution.md
│
├── 02_Engine/                                — Volume II
│   ├── TG-002_Software_Architecture.md
│   ├── TG-003_Project_Structure.md
│   ├── TG-004_Chronology.md
│   ├── TG-005_World_State.md
│   ├── TG-006_Rule_Engine.md
│   ├── TG-007_Causality_Engine.md
│   └── TG-008_Memory_History.md
│
├── 03_Sciences/
│   ├── 01_Natural/                          — Volume III (TG-100 – TG-180)
│   ├── 02_Life/                             — Volume IV  (TG-200 – TG-290)
│   ├── 03_Cognitive/                        — Volume V   (TG-300 – TG-390)
│   ├── 04_Social/                           — Volume VI  (TG-500 – TG-590)
│   └── 05_Civilization/                     — Volume VII (TG-600 – TG-690)
│
├── 04_Story/                                 — TG-STRY-000 – TG-STRY-090
├── 05_Observatory/                           — TG-OBS-000 – TG-OBS-009
├── 06_Development/                           — TG-DEV-000 – TG-DEV-009
│
├── ADR/                                       — Architecture Decision Records (currently empty)
├── RFC/                                       — Proposed features/discussions pre-adoption (currently empty)
├── Archive/                                   — Deprecated/superseded specs (currently empty)
│
├── SPEC_INDEX.md                              — this file
└── DEVELOPMENT_PLAN.md                        — day-to-day execution plan derived from this library
```

**Note on the `03_Sciences` taxonomy:** the Natural/Life/Cognitive/Social/Civilization
five-way split is a *folder-level* organizing scheme. No document in `01_Foundations` or
`02_Engine` defines or names this taxonomy explicitly — it is only visible from the disk
layout and from each science document's own "Volume" self-label.

---

## Dependency Graph

```
TG-000 Vision
   └── TG-001 Constitution
          └── TG-002 Software Architecture
                 └── TG-003 Project Structure
                        └── TG-004 Chronology
                               └── TG-005 World State
                                      └── TG-006 Rule Engine
                                             └── TG-007 Causality Engine
                                                    └── TG-008 Memory & History
                                                           │
        ┌──────────────────────────────────────────────────┘
        ▼
03_Sciences/01_Natural  (TG-100→180, linear chain, each depends on all prior in-folder docs)
        ▼
03_Sciences/02_Life     (TG-200→290, depends on 01_Natural + in-folder prior docs)
        ▼
03_Sciences/03_Cognitive (TG-300→390, depends on 02_Life + in-folder prior docs)
        ▼
03_Sciences/04_Social    (TG-500→590, depends on 03_Cognitive + in-folder prior docs)
        ▼
03_Sciences/05_Civilization (TG-600→690, depends on 04_Social + in-folder prior docs)
        ▼
04_Story    (TG-STRY-000→090, depends on Civilization + Foundations)
        ▼
05_Observatory (TG-OBS-000→009, depends on Story + Foundations)
        ▼
06_Development (TG-DEV-000→009, translates ALL of the above into engineering tasks)
```

**Cross-references repaired 2026-07-09** (`DEVELOPMENT_PLAN.md` Week 1 Day 4 — all five fixed
and verified by re-grep; no remaining `TG-4xx` hits or off-by-one Volume numerals anywhere in
`specification/`):

| Doc | Was | Fixed to | Problem |
|---|---|---|---|
| `TG-STRY-020_Character_Stories.md` | "TG-440 — Identity & Purpose" | "TG-390 — Identity & Self" | No TG-440 exists; the Story docs were written against an older numbering pass never updated after Cognitive/Social were renumbered. |
| `TG-STRY-040_Legends_Myths.md` | "TG-410 — Culture", "TG-420 — Language" | "TG-530 — Culture", "TG-510 — Language" | Same stale-renumbering issue as above. |
| `TG-570_Economics.md` | "TG-005 — Settlements & Economy" | "TG-DEV-005 — Settlement & Economy Implementation" | The real TG-005 is *World State & Entity Model* (`02_Engine`), not Settlements & Economy — that title only exists in `06_Development`. Re-pointed there with an explanatory note, since no Science-volume doc covers settlements+economy jointly. |
| `TG-690_Historical_Ages_Civilizational_Cycles.md` | Entire "Depends On" Volume list off by one (II→VI instead of III→VII) | Renumbered III→VII | Not just the single "Volume VI" mislabel originally spotted — the whole list was shifted down by one Roman numeral relative to every doc's own self-label. |
| `TG-600_Civilization_Emergence.md` | Same off-by-one Volume list (II→V instead of III→VI) | Renumbered III→VI | **Found during the Day 4 repair pass, not in the original read-through** — the identical bug as TG-690, in the volume's other "depends on everything below" summary doc. |

**This also explains the "missing TG-400 series"**: it was never a reserved gap — it's the
former ID range of what is now `03_Sciences/04_Social`, and the Story documents (now repaired)
carried the old numbers.

**Minor remaining naming variance (not urgent):** `03_Sciences/02_Life`'s own documents
consistently self-label as "Volume IV — Biological Sciences," while cross-references from
other folders (and this index) call it "Life Sciences" (matching the folder name `02_Life`).
Not a broken reference — just a cosmetic synonym drift. Leave as-is unless a future doc-cleanup
pass wants to standardize on one term.

---

## Document Naming Convention

| Prefix | Range | Folder | Subject |
|---|---|---|---|
| `TG-000`–`TG-001` | — | `01_Foundations` | Vision, Constitution |
| `TG-002`–`TG-008` | — | `02_Engine` | Architecture, structure, time, world state, rules, causality, memory |
| `TG-1xx` | 100–180 | `03_Sciences/01_Natural` | Physical world (Volume III) |
| `TG-2xx` | 200–290 | `03_Sciences/02_Life` | Biology (Volume IV) |
| `TG-3xx` | 300–390 | `03_Sciences/03_Cognitive` | Mind (Volume V) |
| `TG-5xx` | 500–590 | `03_Sciences/04_Social` | Society (Volume VI) — *see broken-ref note above for why there's no TG-4xx folder* |
| `TG-6xx` | 600–690 | `03_Sciences/05_Civilization` | Civilization (Volume VII) |
| `TG-STRY-0xx` | 000–090 | `04_Story` | Narrative systems |
| `TG-OBS-00x` | 000–009 | `05_Observatory` | Player-facing UI/UX principles |
| `TG-DEV-00x` | 000–009 | `06_Development` | Engineering implementation plan |

Each numbered document (except `00_Master`) carries a header with **Document ID, Version,
Status, Priority, and Depends On** — treat any document missing these fields as non-canonical
(currently only `00_Master/The_Garden_Project_Blueprint.md` lacks them).

---

## Document Status Legend

| Field | Values seen | Meaning |
|---|---|---|
| **Status** | `Living Document` (all of `01_Foundations`–`05_Observatory`); `Active` (`TG-DEV-001`–`007`); `Completed` (`TG-DEV-008`, `TG-DEV-009`) | "Living Document" = design reference, expected to evolve. "Active"/"Completed" = an implementation-phase tracker, specific to `06_Development`. |
| **Priority** | `CRITICAL` (TG-001, TG-002, TG-003, TG-350) · `FOUNDATIONAL` (most docs) · `ADVANCED` (TG-250, TG-260, TG-270, TG-280 — explicitly deferrable) | CRITICAL = load-bearing for everything downstream. ADVANCED = the document itself states it isn't required for early releases. |
| **Version** | `v0.1` (all Foundations/Engine/Sciences/Story/Observatory docs) · `v1.0` (TG-DEV-000–007) · `v2.0` (TG-DEV-008–009) | Version bumps to v2.0 only occur on a "Completed" implementation doc with a final report attached. |

**Caveat:** "Completed" on `TG-DEV-008`/`009` describes *documentation status*, not full
feature parity with the corresponding design specs — both docs candidly list unresolved
"Known Limitations" (see Change Log and `DEVELOPMENT_PLAN.md`).

---

## Change Log

| Date | Change |
|---|---|
| 2026-07-09 | `blueprint/` folder deleted; `specification/` established as the sole authoritative design reference. |
| 2026-07-09 | `SPEC_INDEX.md.txt` (folder structure did not match reality — described `01_World…07_Development`, actual disk layout is `00_Master…06_Development`) replaced by this file, `SPEC_INDEX.md`. |
| 2026-07-09 | Full read-through of all 84 specification documents completed; findings below folded into this index and into `DEVELOPMENT_PLAN.md`. |
| 2026-07-09 | Confirmed via grep: `TG-STRY-020`/`TG-STRY-040` still cite dead `TG-410/420/440` IDs — root cause of the apparent "missing TG-400 series" identified (old Social-sciences numbering, never updated after renumber to TG-5xx). Not yet repaired — tracked as a doc-fix task. |
| 2026-07-09 | Confirmed two "Known Limitations" from the old (deleted) `blueprint/development/TG-DEV-008/009` carried forward **unchanged** into the new `specification/06_Development/TG-DEV-008/009`: (1) civilization events published to the event bus but never subscribed by `HistorySystem` for archival — contradicts TG-001 Law IV ("History Is Permanent"); (2) the AI narrator/assistant is template-based, not an actual LLM. Both tracked as backlog items in `DEVELOPMENT_PLAN.md`. |
| 2026-07-09 | Confirmed the old blueprint's `TG-DEV-000` self-mislabeling as "TG-DEV-010" is **fixed** in the new specification copy. |
| 2026-07-09 | New finding: `TG-DEV-009`'s "Repository Summary" section still depicts a `blueprint/` folder in its tree diagram — stale relative to the current `specification/`-based layout. Tracked as a doc-fix task. |
| 2026-07-09 | **Week 1 Day 1 (DEVELOPMENT_PLAN.md) shipped and tested:** `HistorySystem` now subscribes to all 12 civilization events (`KingdomFounded`, `KingdomDissolved`, `LeaderElected`, `GovernmentFormed`, `DiplomaticRelationChanged`, `TradeRouteEstablished`/`Abandoned`, `MigrationStarted`/`Completed`, `TechnologyDiscovered`, `ReligionEstablished`, `CulturalFestivalHeld`) — closing the TG-001 Law IV violation. 5 new `HistoryCategories` added (Politics/Diplomacy/Religion/Culture/Technology). `SignificanceEvaluator`'s High-importance set extended for founding/breakthrough events. 14 new unit tests added (`HistorySystemCivilizationEventTests.cs`); full suite verified: 50/50 unit tests pass, 4/4 integration tests pass (7m26s — unchanged from baseline, confirming no regression). |
| 2026-07-09 | **Week 1 Day 2 shipped and tested:** moved `docker/nginx/nginx.conf` into `src/Garden.Observatory/nginx.conf` (was outside the Observatory service's Docker build context) and fixed the Dockerfile's `COPY` path. Verified with an actual `docker compose build` — both `api` and `observatory` images now build successfully (previously `observatory` failed outright). |
| 2026-07-09 | **Week 1 Day 3 shipped and tested:** `ADR/ADR-001-empty-project-disposition.md` written. `Garden.Simulation`, `Garden.Story`, and `Garden.Tools` (fully orphaned, zero references anywhere) removed from `TheGarden.slnx` and deleted; `Garden.Contracts`/`Garden.Shared` retained as reserved namespaces (still live `ProjectReference` targets). Solution now has 7 `src/` projects instead of 10. Verified: full solution builds clean, all tests still pass. |
| 2026-07-09 | **Week 1 Day 4 shipped and tested:** repaired the four broken cross-references above, plus one additional instance of the same off-by-one Volume-numbering bug found in `TG-600_Civilization_Emergence.md` during the repair pass (not caught in the original read-through). Verified by re-grep: zero remaining `TG-4xx` dead references, zero remaining off-by-one Volume numerals in `specification/`. |
| 2026-07-09 | **Week 2 Day 6 shipped and tested:** TRX-profiled `Garden.IntegrationTests` and found `Population_RemainsViable_AcrossThreeYears` alone accounts for ~6m12s of the suite's ~7m20s (3 simulated years vs. 2 months for its siblings). Tagged it `[Trait("Category","Slow")]`; the other three tests tagged `Fast`. Verified: `--filter "Category!=Slow"` cuts the run from 7m20s to 1m10s with the same pass count. |
| 2026-07-09 | **Week 2 Day 7 shipped and tested:** split `.github/workflows/ci.yml` into `unit-tests` (blocking), `integration-tests-fast` (blocking, `Category!=Slow`, ~1m), and `integration-tests-full` (push to main/develop + nightly only, never on `pull_request`). Verified: YAML parses correctly (via `js-yaml`) and both new job commands run correctly locally with the expected test counts/timings. |
| 2026-07-09 | **Week 2 Day 8 shipped and tested:** added `Settlement.Tier` (Hamlet→Village→Town→City→RegionalCapital→Metropolis, population-threshold-derived per `TG-650`) and surfaced it in `/settlements` + `/settlements/{id}` and the Observatory UI. Verified against a live running simulation (not just unit tests) — confirmed real settlements correctly tiered as Village/Hamlet in both the API JSON and a live screenshot of the Settlements page. 12 new unit tests. Found and flagged (not fixed, out of scope) a pre-existing bug: `SettlementsController.GetAll()` sends the raw `GameEntityId` object instead of `.ToString()`, breaking the detail-panel click-through — spawned as a separate task. |
| 2026-07-09 | **Week 2 Day 9 shipped and tested:** added `Settlement.AuthoritySource`/`Legitimacy`/`LastGovernmentChangeTick` per `TG-580`; `GovernanceService` now derives authority source (Competence/Election/Tradition) from government tier and computes Legitimacy from the real leader's ContributionScore/Reputation plus time-since-last-transition; `DiplomacyService` now applies a small penalty when either settlement's legitimacy is low. **Two real bugs found and fixed during verification, not just at review time:** (1) `HistorySystem.OnLeaderElected` (Day 1) only checked `IsNullOrEmpty(PreviousLeaderName)`, but `LeadershipService` actually publishes the literal string `"None"` for a settlement's first leader — every real first-leader election was being mislabeled as "succeeding None" and wrongly archived; fixed and regression-tested. (2) Adding the new `Settlement` fields without an EF Core migration crashed the API on startup the moment it queried `Settlements` from Postgres (`column s.AuthoritySource does not exist`) — this is what was silently breaking every `garden-api` preview restart, not a tool flakiness as first suspected; fixed by generating and applying migration `AddSettlementGovernanceFields`. Also fixed a self-healing gap: `AuthoritySource` was only set on a government *transition*, so settlements that had already reached their tier before this shipped would never get it backfilled — moved to unconditional recomputation, verified live against a resumed save. 9 new unit tests; 72/72 unit tests passing; confirmed live via a real running/resumed simulation end-to-end. |
| 2026-07-09 | **Week 2 Day 10 shipped:** `RFC/RFC-001-emotion-system.md` written, scoping Week 3's Emotion system — 6 of TG-330's 15 named emotions (Fear/Joy/Sadness/Trust/Curiosity/Loneliness), an invented decay-half-life table, a concrete data model matching the existing `PersonalityTraits`/`CitizenNeeds` convention, and narrow additive integration points into `CitizenSystem`'s existing decision chain rather than a full decision-loop rewrite. |
| 2026-07-09 | **Fixed the Day-8-flagged `SettlementsController` ID bug:** `GetAll()`, `GetById()` (including its nested `members`/`Buildings` projections), and `GetBuildings()` now all send `Id = x.Id.ToString()` instead of the raw `GameEntityId` struct. Verified live end-to-end — restarted the API, reloaded the Observatory, clicked a settlement row, and confirmed the detail sheet now loads real data (Tier, Leader, Government, Authority, Legitimacy, Religion) instead of 400ing. **Found the same bug is far more widespread while doing the requested grep**: `CivilizationController`, `HistoryController`, `MemoriesController`, and `StoriesController` all have raw `GameEntityId` projections too (every domain entity's `Id` is `GameEntityId`). Fixing those was out of scope for this specific request, so it's spawned as a separate follow-up task rather than silently expanding scope. |
| 2026-07-09 | **Fixed the widespread version of the GameEntityId serialization bug** across all four flagged controllers: `CivilizationController.cs` (18 fixes across `GetKingdoms`/`GetKingdomDetail`/`GetGovernments`/`GetLeaders`/`GetDiplomacy`/`GetTradeRoutes`/`GetTechnology`/`GetCulture`/`GetReligion`/`GetMigration`), `HistoryController.cs` (3 fixes — plus one found beyond the original report: `GetById` returned the raw `HistoricalRecord` entity directly rather than a projection, so it had the identical symptom even though it doesn't match the `x.Id,` pattern originally grepped for; rewritten as an explicit projection), `MemoriesController.cs` (3 fixes across citizen/family/collective memories), `StoriesController.cs` (2 fixes). Confirmed `SearchController.cs` was already correct via a different but equally valid pattern (`Id = x.Id.Value`, a `Guid` — also serializes as a plain string). Verified: full solution builds clean, 72/72 unit tests pass, and live-checked against the running simulation — `governments`, `leaders`, `culture` (including nested `traits`), `history`, and `stories` endpoints all confirmed returning plain string IDs. `kingdoms`/`diplomacy`/`trade-routes`/`religion` had no data yet in the live world to check against (game-state conditions not met, not a bug) but are structurally identical fixes, already confirmed by the code sweep. Also confirmed via `grep` that no other controller has this bug. As a side effect, this also fixes React "duplicate key" console warnings on the History page (`key={story.id}`/`key={record.id}` were literally `[object Object]` for every list item before this fix). |
| 2026-07-09 | **Week 3 Days 11-12 shipped and tested:** `Citizen.Emotions` (`EmotionalState`: Fear/Joy/Sadness/Trust/Curiosity/Loneliness) implemented per RFC-001, owned by a new `EmotionSystem`. Each emotion has a real trigger (Needs-criticality for Fear, `SettlementFounded`/`CitizenBorn` events for Joy, `CitizenDied` for Sadness among settlement-mates, `Reputation` for Trust, `Personality.Curiosity` for Curiosity, `HomeSettlementId` for Loneliness) and decays via a shared half-life formula so nothing gets pinned at an extreme. Required an EF Core migration (`AddCitizenEmotions`) since `Emotions` is a new owned type on the persisted `Citizen` entity — generated and applied *before* first live test this time, learning directly from the Day 9 crash-on-restart lesson. 10 new unit tests; verified live against the running/resumed simulation (Trust/Curiosity visibly converging toward their real targets per citizen). |
| 2026-07-09 | **Week 3 Day 13 shipped and tested:** pairwise `Relationship` entity (Trust/Affection/SocialDistance) per TG-380, owned by a new `RelationshipSystem`, created lazily on real interaction events (`CitizenBorn` for parent-bonding; `TradeCompletedEvent` also wired but currently dormant — that event type is defined but never actually published anywhere in this codebase, a pre-existing gap unrelated to this work). No EF migration needed — `Relationship` follows the same pure in-memory `WorldState` list pattern as `DiplomaticRelation`/`TradeRoute`/`Kingdom`/`Religion` (only `Citizen` and `Settlement` are actually EF-persisted). 6 new unit tests. Live verification of the `CitizenBorn` trigger specifically was blocked by the resumed save's settlements all being below the 3.0 food-per-capita reproduction threshold (a real game-state condition, not a bug) — mechanism confirmed via exact-value unit tests instead. |
| 2026-07-09 | **Week 3 Day 14 shipped and tested:** narrow additive hooks wiring Emotion/Relationship into `CitizenSystem`'s existing decision chain per RFC-001 — high Fear lowers the effective critical-need thresholds (a citizen already afraid acts sooner), and high Loneliness adds a `Socialize` goal. **Found and fixed a real design bug during this work**: the first placement of the Loneliness hook required `HomeSettlementId != null`, but `EmotionSystem` only targets high Loneliness for citizens *without* a settlement — the two conditions could almost never both be true. Relocated `Socialize` to the bottom fallback (reached by a homeless citizen who couldn't find or found a settlement this tick either), matching where `Loneliness` is actually elevated. 6 unit tests, all fast integration tests still passing, and live-confirmed the priority ordering is correct: three real citizens in the running simulation had `Loneliness=60` (past the 40 threshold) but correctly kept pursuing higher-priority goals (`FindWater`/`TravelToSettlement`/`JoinSettlement`) instead of being wrongly overridden into `Socialize`. |
| 2026-07-09 | **Week 3 Day 15 (close-out) shipped:** added `EmotionRelationshipIntegrationTests.cs` — runs `EmotionSystem`/`RelationshipSystem` alongside the full existing system stack (citizens, settlements, economy) over 2 simulated months, not just in isolated unit-test harnesses; confirms population survives and at least one citizen's Emotions measurably diverge from defaults. `TG-330_Emotion.md` and `TG-380_Relationships.md` both given an "Implementation note" pointing to `RFC-001`/the new systems, listing exactly which of their named concepts are and aren't implemented yet. `RFC-001` marked Implemented, noting both of its open questions were resolved as recommended. **Week 3 final tally:** 22 new unit tests (94 total, up from 72 at the start of the week) + 1 new integration test (4 fast total), full suite passing, `EmotionSystem`/`RelationshipSystem` both registered in the tick scheduler and confirmed live against the running/resumed simulation. |
| 2026-07-09 | **Week 4 Day 16 shipped and tested:** Settlement Tier/Governance surfaced "properly" per TG-OBS-002 rather than as a raw field dump — `GovernanceService.GetLegitimacyBreakdown` exposes Competence/PublicTrust/Stability as a public method (not just the combined total), wired through `SettlementsController` and rendered in the Observatory with one-line explanations of what Tier/Authority/Legitimacy actually mean (Principle 9, Explainability). 1 new unit test; verified live — a real settlement's breakdown (Competence 0, Trust 50, Stability 100 → Overall 45) matched the UI exactly. |
| 2026-07-09 | **Week 4 Day 17 shipped and tested:** faceted History Explorer search per TG-OBS-005's "Historical Search" section — `HistoricalArchive.Search` gained a `settlementId` filter and keyword matching now also searches participant names (Person facet); a new `/history/facets` endpoint lists real categories/settlements for filter UI instead of hardcoding them; `StoriesController.GetAll` gained category/keyword filters. Family is explicitly *not* faked as a facet - no family data model exists anywhere in this codebase. 5 new unit tests; verified live end-to-end (dropdowns populate from real data, filtering by "Harvest" theme correctly narrows the result set). |
| 2026-07-09 | **Week 4 Day 18 shipped and tested:** audited `SignificanceEvaluator` against TG-STRY-050's Core Principle ("consequences, not spectacle") and found `FarmHarvested` was hardcoded to always archive as High importance regardless of actual yield - directly observed live during Day 17 testing as dozens of near-duplicate "Harvest at X" records flooding the archive for one settlement within a few in-game days. Removed it from the forced-High whitelist and scaled its severity by actual `Yield` in `HistorySystem.OnFarmHarvested`, so only genuinely large harvests are archived. Documented `TradeCompleted`'s identical latent issue as a known, currently-harmless follow-up (that event is never actually published anywhere yet). 3 new unit tests; verified live - a ~103-unit harvest now correctly archives as Medium instead of unconditional High, and small harvests stop being archived at all. |
| 2026-07-09 | **Week 4 Day 19 shipped and tested:** basic accessibility pass on the Observatory per TG-OBS-008 - a reduced-motion toggle (persisted, plus automatic `prefers-reduced-motion` support), color-independent status indicators (an icon alongside `WellbeingBar`'s color; Citizens table's Hunger/Energy bars gained the numeric values Health already had, not just color), and keyboard navigation (clickable Citizen/Settlement rows are now real focusable, Enter/Space-activatable controls with `aria-label`s; sortable column headers converted from bare `<th onClick>` to real `<button>` elements). No accessibility tooling exists in the stack, so this was spot-checked manually and live — confirmed the reduce-motion class toggles correctly, confirmed Enter-key activation opens the correct citizen's detail panel, and confirmed the previous verification method itself had a flaw (checking a `data-slot="sheet-content"` attribute that doesn't exist in this codebase's actual `Sheet` component) before trusting a false negative. |
| 2026-07-09 | **Week 4 Day 20 (sprint close):** Weeks 1-4 of `DEVELOPMENT_PLAN.md` are complete. See the Change Log entries above for the full day-by-day record; see "Backlog Triage (2026-07-09)" below for what's next. |
| 2026-07-10 | **Week 5 Day 21 shipped and tested:** `Citizen.KnownEventIds` (`List<string>`) added per `RFC-002`, mapped as a Postgres `text[]` with an EF Core `ValueComparer` (same pattern as `Settlement.MemberIds`). Migration `AddCitizenKnownEvents` initially **failed live** against the populated database — a `NOT NULL` array column with no default fails on existing rows (`SqlState 23502`) — fixed with `defaultValueSql: "ARRAY[]::text[]"` and reverified successfully against the real 113-citizen dataset. |
| 2026-07-10 | **Week 5 Day 22 shipped and tested:** `CommunicationSystem` (new `IScheduledSystem`, daily cadence) subscribes to `TechnologyDiscoveredEvent`/`KingdomFoundedEvent`/`ReligionEstablishedEvent`, marks the discoverer/founder as knowing, and diffuses knowledge across the `Relationship` graph (SocialDistance < 40, Trust > 30 gate, both per RFC-002). **Found and fixed a real gap while implementing:** `TechnologyDiscoveredEvent` and `ReligionEstablishedEvent` never actually exposed a citizen ID, even though `TechnologyService`/`ReligionService` already computed a discoverer/founder internally — added `DiscoveredByCitizenId`/`FounderCitizenId` to the two events, populated from the values those services already had. `KnownEventIds` stores a synthetic key (`"Technology:{id}"` etc.), not `HistoricalRecord.Id` — `HistorySystem` never exposes its generated archive record back to other subscribers of the same event, so matching by search would be fragile. Registered in DI and the scheduler alongside `EmotionSystem`/`RelationshipSystem`. |
| 2026-07-10 | **Week 5 Day 23 shipped and tested:** `CommunicationSystemTests.cs` — 9 new unit tests covering the positive path (marking + diffusion for all three event types) and the negative gates (SocialDistance too far, Trust too low, dead citizen, event with no citizen ID). Live organic verification of Kingdom/Religion/Technology milestones was attempted first (ran the simulation ~56 in-game years at up to 5000x) but found unreliable: Kingdom/Religion formation are chance-gated per year, and **Technology discovery is currently unreachable in practice** due to a pre-existing bug — `TechnologyService.EvaluateTechnology()` scales each individual tech's progress at 10% of the settlement-level counter, making even the cheapest technology require ~385 simulated years for a 26-member settlement. Flagged as a new backlog item (`task_7f002f15`), not fixed (out of RFC-002 scope). Verified the propagation logic directly instead, publishing domain events onto the bus — same approach `EmotionSystemTests.cs` used for RFC-001's rare triggers. 112/112 unit tests, 3/3 fast integration tests passing. |
| 2026-07-10 | **Week 5 Day 24 shipped and tested:** minimal read-only "What This Citizen Knows" surfacing per RFC-002's explicit UI scope limit — `CitizensController.GetCitizen` resolves each `KnownEventIds` key against `CommunicationSystem.EventTitles` and returns `{key, title}` pairs; rendered as a new section in the Observatory's citizen detail panel. Verified live: API returns the correctly-shaped (currently empty, since no milestone has fired yet) array; UI renders the empty state ("Hasn't heard about any civilization events yet.") correctly, confirmed via `document.body.innerText` and a screenshot; `tsc --noEmit` clean. |
| 2026-07-10 | **Week 5 Day 25 (close-out) shipped:** `RFC-002` marked Implemented with an added "Implementation notes" section documenting the synthetic-key decision, the two event-field additions, and the live-verification limitation. `DEVELOPMENT_PLAN.md`'s Week 5 table updated with actuals; Backlog table updated (Communication struck through as shipped, `TechnologyService` scaling bug added). **Week 5 final tally:** 112 unit tests (up from 103), 3 fast integration tests, full solution build clean, live-verified end-to-end (API + Observatory UI). |
| 2026-07-10 | **Fixed the Day-22-flagged `TechnologyService` progress-scaling bug:** `EvaluateTechnology()`'s per-technology `contribution` no longer scales down by 0.1x relative to `settlement.TechnologyProgress` (category multipliers for Agriculture/Construction retained on top). 3 new unit tests (115 total) assert a technology discovers within a bounded number of yearly evaluations. Verified live on a fresh simulation run: 10 technologies discovered across 2 settlements (Deepridge, Upperridge) within Year 1 alone — versus zero after 55+ simulated years before the fix. |
| 2026-07-10 | `RFC/RFC-003-language-divergence.md` written, scoping Week 6's Language increment — a pairwise `LanguageDivergence` score (0-100) between settlements, driven by real contact (`TradeRoute`/`DiplomaticRelation`) or isolation, with `DialectFormedEvent` as the one TG-510 event this increment makes real. No named-Language entity yet (see the RFC's "Why no named Language entity yet" section) — deliberately deferred alongside Vocabulary/Grammar/Writing/Prestige/Translation. |
| 2026-07-10 | **Week 6 Days 26-27 shipped and tested:** `LanguageDivergence` entity + `LanguageSystem` (new `IScheduledSystem`, yearly cadence matching `TechnologyService`/`ReligionService`/`KingdomService`) implemented together — contact (an active `TradeRoute` or `DiplomaticRelation` with `RelationScore > 50`) converges Divergence toward 0, isolation diverges it toward 100 at 2x the rate, and `DialectFormedEvent` fires exactly once when a pair crosses 70. Registered in DI and the scheduler. |
| 2026-07-10 | **Week 6 Day 28 shipped and tested:** `LanguageSystemTests.cs` — 6 new unit tests (121 total) covering no-row-by-default, trade-route contact converging, isolation diverging, `DialectFormed` firing exactly once, a positive `DiplomaticRelation` counting as contact on its own, and canonical settlement-pair ordering. |
| 2026-07-10 | **Week 6 Day 29 shipped and tested:** minimal "most-diverged/most-converged neighbor" surfacing — `SettlementsController.GetById` returns each tracked `LanguageDivergence` row sorted by Divergence ascending; rendered as a new "Language" section in the Observatory's settlement detail panel. **Found two real, separate pre-existing bugs during live verification, neither fixed here (out of RFC-003 scope):** (1) `TradeRouteService.EvaluateTradeRoutes()` never creates any route even when its own stated conditions are clearly met (confirmed live: Food 74 vs 0 between two settlements 23 tiles apart, well within its own 25-tile/surplus thresholds) — flagged via `spawn_task` (`task_b82147bd`); (2) `CivilizationSystem`'s `_lastYearlyTick >= 336` cadence (shared by Technology/Religion/Kingdom/Culture/Language) is actually ~14 days, not a year, per `SimulationTime`'s real scale (8640 ticks/year) — noted in the Backlog table, affects five systems at once. Diplomatic relations separately couldn't form either, but for a legitimate reason (no settlement pair in this world is within the 15-tile threshold — not a bug). Verified the Language mechanic itself via `LanguageSystemTests.cs` instead; verified the UI renders correctly (no crash, correct absence of the section) with real settlement data that simply has no divergence tracked yet. |
| 2026-07-10 | **Week 6 Day 30 (close-out) shipped:** `RFC-003` marked Implemented with an added "Implementation notes" section documenting the two findings above and confirming the recommended divergence/convergence rate ratio was adopted as-is. `DEVELOPMENT_PLAN.md`'s Week 6 table updated with actuals; Backlog table updated (Language struck through as shipped, both new findings added as separate backlog rows). **Week 6 final tally:** 121 unit tests (up from 115), 3 fast integration tests, full solution build clean. |
| 2026-07-10 | **Fixed the Day-29-flagged `TradeRouteService` bug:** root cause was that `EvaluateTradeRoutes()`'s `existing != null` check unconditionally skipped a settlement pair forever once any route existed for it, active or not — a route going quiet once (an ordinary occurrence as local surpluses come and go) permanently locked that pair out of ever trading again, even when a fresh surplus/scarcity later made it exactly as trade-worthy as any other pair. A secondary bug was found and fixed alongside it: `EstablishRoute` always moved goods from the first settlement argument to the second, regardless of which one actually held the surplus (`FindTradeGood` matches either direction) — direction is now determined from the real surplus holder. 3 new unit tests (124 total), including an exact reproduction of the live numbers originally reported (Food 74 vs 0, 23 tiles apart — passed even before the fix, confirming the creation logic itself was sound) and a dedicated reactivation-after-abandonment test (failed before the fix, passes after). **Live re-verification was inconclusive**, not confirmed: a fresh simulation run's settlements repeatedly sat exactly at the `FindTradeGood` boundary (Food = 10, the rule needs strictly <10) rather than crossing it during the session's time budget — a separate resource-equilibrium detail worth being aware of, not chased further here. |
| 2026-07-10 | **Anomaly audit requested by the user** ("are there untracked breadcrumbs or leftover bugs?") — swept the codebase for `TODO`/`FIXME`/`HACK` comments (none found; this project tracks findings in this Change Log/`DEVELOPMENT_PLAN.md` instead), confirmed the working tree was clean with nothing uncommitted, and cross-checked every `CivilizationEvent`-derived type against `HistorySystem`'s subscriptions. **Found three real, previously untracked issues**, all added to the Backlog table: (1) `DialectFormedEvent` (Week 6) was never added to `HistorySystem`'s subscriptions alongside the other 12 `CivilizationEvent` types — a regression of the exact TG-001 Law IV violation Week 1 Day 1 was created to close, just on new code; (2) `Citizen.Emotions` (Week 3) is returned by the API but never rendered anywhere in the Observatory (confirmed via grep — zero references to "emotions" in `Garden.Observatory/src`); (3) the `GET /citizens/{id}/relationships` endpoint (Week 3) is never called by the frontend (confirmed via grep — no "relationship" reference in `CitizensPage.tsx` besides an unrelated placeholder field). None fixed yet — all three are small, contained, and follow existing patterns, but weren't in this specific request's scope. |
| 2026-07-10 | **Week 7 (Anomaly Cleanup) shipped and tested, consolidating the three audit findings above into a day-to-day plan rather than loose backlog rows:** (Day 31) `HistorySystem` now subscribes to `DialectFormedEvent`, archiving it under `HistoryCategories.Culture` (no dedicated Language category exists — `CulturalFestivalHeld` already set that precedent); (Day 32) `Citizen.Emotions` now renders as a new section in the Observatory's citizen detail panel, mirroring the existing Attributes/Personality grid style; (Day 33) a new `GET /citizens/{id}/relationships` call surfaces Trust/Affection/SocialDistance per relationship, mirroring `SettlementsPage`'s Language section from Week 6 Day 29. 1 new unit test (125 total); Emotions verified live against real data (Curiosity 35.2). **Relationships could not be verified against real data** — `Relationship` is pure in-memory (resets on every API restart, same as `Kingdom`/`DiplomaticRelation`/`TradeRoute`), and this session's resumed world had every settlement below the 3.0 food-per-capita reproduction threshold (the identical real, non-buggy blocker Week 3 Day 13 already documented) — verified instead via existing `RelationshipSystemTests.cs` coverage and confirming the UI renders cleanly (no crash, no console errors, correct conditional absence) with genuinely empty data. |
| 2026-07-10 | `RFC/RFC-004-education-apprenticeship.md` written, scoping Week 8's Education increment — informal apprenticeship (one mentor, one student) via the existing `Relationship` graph and `Citizen.Attributes.Intelligence`, deliberately not waiting on Groups/Social Norms (both zero-code-footprint), following RFC-002/003's precedent for not blocking on unimplemented dependencies. Also notes `TechnologyService`'s existing dead `intelligenceFactor` stub as the natural (but deferred) next integration point. |
| 2026-07-10 | **Week 8 (Education: Apprenticeship) shipped and tested:** `Apprenticeship` entity + `EducationSystem` (yearly cadence, matching the `TechnologyService`/`ReligionService`/`KingdomService`/`LanguageSystem` convention) pairs an Adult/Elder mentor with a Child/Teen student wherever a close `Relationship` (SocialDistance < 40, reused from RFC-002/003) and a ≥2.0 Intelligence gap exist, gradually raising the student's Intelligence toward the mentor's; `ApprenticeshipStarted`/`Completed` events fire on pairing and on completion (gap closes, or the relationship decays past the contact threshold). 8 new unit tests (133 total). Minimal Observatory surfacing added: a citizen's "Mentoring X" / "Learning from X" line in the detail panel. **A real structural finding, not fixed here:** `RelationshipSystem`'s only live trigger bonds a newborn's two parents, never parent-and-child — meaning `EducationSystem`'s mentor/student gate can never organically trigger yet, not merely rarely. Noted in the Backlog table as a `RelationshipSystem` follow-up. Verified via `EducationSystemTests.cs`'s synthetic pairs and confirming the UI handles empty data cleanly (no crash, no console errors). |
| 2026-07-10 | `RFC/RFC-005-law-dispute-resolution.md` written, scoping Week 9's Law & Justice increment — informal dispute resolution (citizen pair, same settlement) via the settlement's existing leader and `Legitimacy` score, deliberately not waiting on Social Norms (zero-code-footprint), following RFC-002 through RFC-004's precedent. Notes the one-directional relationship with `GovernanceService`'s existing `Legitimacy` field, mirroring how RFC-004 read (but didn't write into) `TechnologyService`'s `intelligenceFactor`. |
| 2026-07-10 | **Week 9 (Law & Justice: Dispute Resolution) shipped and tested:** `LegalCase` entity + `LawSystem` (yearly cadence, same convention) opens a case wherever a `Relationship.Trust < 20` exists between two citizens sharing a `HomeSettlementId`, resolves it with a chance equal to `settlement.Legitimacy / 100.0` (restoring some Trust on success), or closes it as a `JusticeFailureEvent` after 3 unresolved yearly evaluations. 7 new unit tests (140 total) — one initially asserted a persisting dispute would only ever produce one total case, which the code correctly contradicted (a dispute that never actually resolves legitimately reopens after each failure, since TG-590 doesn't rule this out); the test was corrected, not the code. Minimal Observatory surfacing added: a settlement's open/resolved/failed case counts. **A second instance of Week 8's structural finding:** `RelationshipSystem` only ever applies *positive* Trust deltas — nothing lowers Trust, so a genuine dispute is currently just as unreachable organically as Week 8's mentor/student gate. Both findings now share one Backlog row, since fixing `RelationshipSystem`'s trigger set would unblock both increments' live verification at once. Verified via `LawSystemTests.cs`'s synthetic disputes and confirming the Observatory UI handles empty data cleanly. |
| 2026-07-10 | `RFC/RFC-006-life-sciences-flora-history.md` written, scoping the first increment of Volume IV (Life Sciences) — investigation before scoping found `HistorySystem` subscribes to zero `EnvironmentalEvent` types, even though `EcologySystem` already publishes real, rare, gated `ForestExpandedEvent`/`ForestDeclinedEvent` that map directly onto TG-210 Flora's own named events. Scopes wiring just those two (not the high-frequency `ResourceRegeneratedEvent`, to avoid repeating the Day-18 `FarmHarvested` archive-flooding mistake); explicitly does not touch `AgricultureSystem`'s crop-growth hardcoding, the volume's real "untangling" question. |
| 2026-07-10 | **Week 10 (Life Sciences: Flora History) shipped and tested:** added `HistoryCategories.Nature`; `HistorySystem` now subscribes to `ForestExpandedEvent`/`ForestDeclinedEvent`, both previously dead since before this development cycle began. 2 new unit tests; verified live — 50 `Nature` records archived within Year 1 of a fresh run, all matching real terrain transitions. **A second, more significant, unrelated finding surfaced during verification:** `HistorySystem.Archive()` — the shared method under all ~25 event handlers, not just Flora's two — hardcoded `LocationY = locationX + 1`, silently discarding the real Y coordinate for every historical record archived across all nine prior weeks. No test had ever asserted on `LocationY`. Fixed by giving `Archive()` separate `locationX`/`locationY` parameters and updating all 25 call sites; a new regression test (`ArchivedRecord_StoresTheRealLocationY_NotLocationXPlusOne`) locks it in. Verified live: a real record's `locationY` (49) now independent of `locationX` (15), matching the actual tile. **Day 49 assessment:** Week 11 should not default to continuing Volume IV — the next slice (Population Ecology) immediately hits the still-unresolved `AgricultureSystem` hardcoding question this week deliberately avoided; recommended either that ADR or pivoting to Borders & Territorial Dynamics instead, left open in `DEVELOPMENT_PLAN.md` for whoever picks up Week 11. **Week 10 final tally:** 143 unit tests (up from 140), 3 fast integration tests, full solution build clean. |
| 2026-07-10 | **Decision:** Week 11 picks Borders & Territorial Dynamics over the `AgricultureSystem` ADR — doesn't block on resolving that question, keeps the weekly RFC cadence moving. `RFC/RFC-007-borders-territorial-influence.md` written, scoping a regional-influence field from existing Population/Legitimacy, replacing the flat ever-growing `TerritoryRadius`, plus dispute *detection* only (no resolution mechanic). |
| 2026-07-10 | **Week 11 (Borders & Territorial Dynamics: Territorial Influence) shipped and tested:** `Settlement.TerritorialInfluence` (new EF migration `AddSettlementTerritorialInfluence`, applied live) + `TerritorySystem` (yearly cadence) computes influence from Population/Legitimacy; a rise beyond ±10 expands territory (reusing the existing `ExpandTerritory` path), a fall contracts it (`BorderContractedEvent`); overlapping settlements of comparable influence (within 20%) trigger `BorderDisputeBeginsEvent` once per pair. **Both new events were subscribed to `HistorySystem` at introduction time** — the same TG-001 Law IV pattern that had to be fixed after the fact for `DialectFormedEvent` (Week 7) and `ForestExpanded`/`Declined` (Week 10) was caught and closed proactively this time, during this week's own live verification, before it became a third instance of the same mistake. 8 new unit tests for `TerritorySystem` plus 2 more for the `HistorySystem` wiring (153 total). Verified live: `TerritorialInfluence` computed correctly from real data (23.5, rendered as 24 in the Observatory); no `BorderContracted`/`BorderDisputeBeginsEvent` occurred organically within the verification window, but both are gated by thresholds simply not crossed in this run, not bugs — confirmed via `TerritorySystemTests.cs`'s direct scenarios. Full solution build clean, 3/3 fast integration tests passing. |
| 2026-07-13 | **Week 12 (Anomaly Cleanup 2: RelationshipSystem + CivilizationSystem Cadence) shipped and tested, consolidating the two open findings sitting in the Backlog table since Weeks 8-10:** (Day 56) `RelationshipSystem.OnCitizenBorn` now also bonds each parent with the newborn (previously only bonded the two parents with each other), unblocking `EducationSystem`'s mentor/student gate for the first time since Week 8; (Day 57) added `RelationshipSystem.OnCitizenDied` — a citizen's close mourners (existing Relationship, Affection > 60) get Trust lowered on their *other* existing relationships, the project's first mechanic capable of pushing Trust below the neutral baseline, unblocking `LawSystem`'s dispute detection since Week 9; (Day 58) added `SimulationTime.TicksPerYear` (`24 * 30 * 12 = 8640`) as a single source of truth and replaced every hardcoded `336`-tick "year" (`CivilizationSystem`, `EducationSystem`, `LanguageSystem`, `LawSystem`, `TerritorySystem`, and the tests simulating yearly ticks) with it — the Week 6 Day 27 finding that this cadence was actually ~14 days, not a year, affecting eight systems at once. **One existing test broke and was fixed, not the code**: `LawSystemTests.FailsAsJusticeFailure_WhenLegitimacyIsZero_AfterUnresolvedWindow` hardcoded the old 336-tick "year" directly. 3 new `RelationshipSystemTests` (156 total). **Day 59 live re-verification**: ran the resumed simulation ~3 in-game years at 500x speed (food reserves were critically low across most settlements, a real, non-buggy scarcity condition) — no organic `CitizenDied`/`ApprenticeshipStarted`/`CaseResolved`/`JusticeFailure`/`TechnologyDiscovered` event occurred in that window. Consistent with the Week 11 Day 54 precedent (`BorderDispute` non-occurrence): these are probability/threshold-gated mechanics that simply weren't crossed in this particular run, not bugs — the underlying mechanisms are directly unit-tested. **A separate, previously-unnoticed issue was found (not fixed, out of Week 12 scope) while querying `/History/search` during this verification**: its `totalRecords` field under-reports relative to the actual `records` array length returned (e.g. reported `50` while 500-1000 real records came back) — flagged to the Backlog table. Full solution build clean, 156/156 unit tests, 3/3 fast integration tests (1m15s). |
| 2026-07-13 | **Day 61 leftover consolidation sweep (user-requested):** re-checked the Backlog table and every RFC's "not fixed here" notes for anything small and contained still open. Found and fixed two: (1) `SeasonChangedEvent` (published by `SeasonSystem` since before this development cycle, never subscribed by `HistorySystem`) — a fourth instance of the same TG-001 Law IV violation Week 1 Day 1, Week 7 Day 31, and Week 10 Day 46 each had to fix; wired it under `HistoryCategories.Nature`, 1 regression test, verified live (a real "Summer Begins" record archived at tick 2161); (2) the Day 59-found `History/search` `totalRecords` bug — root cause was `HistoricalArchive.Search()`'s `take` parameter defaulting to 50 and `HistoryController`'s total-count call never overriding it; fixed with `take: int.MaxValue`, 1 new unit test, verified live (`totalRecords` now correctly reports 478 against a `pageSize=10` query, no longer capped at 50). Confirmed via `grep` that `src/` has zero `TODO`/`FIXME`/`HACK` comments (expected — this project tracks findings here instead). The other 7 `EnvironmentalEvent` types RFC-006 named (`RainStarted`/`Stopped`, `RiverExpanded`/`Shrank`, `LakeDried`, `DroughtStarted`/`Ended`) and `TradeCompletedEvent`'s dead-code status were both re-checked and confirmed unchanged — genuinely still out of scope (no publisher to wire against, or needs an invented mechanic respectively), not silently-forgotten quick fixes. **Week 12 final tally revised:** 158 unit tests (up from 153), 3 fast integration tests, full solution build clean. |
| 2026-07-13 | **Week 13 (The `AgricultureSystem` Crop-Growth ADR) shipped:** `ADR/ADR-002-agriculture-system-crop-growth-disposition.md` resolves the Week 10 Day 49 backlog item that had blocked Population Ecology for two cycles. Decision: keep `AgricultureSystem`'s season/moisture yield formula exactly as-is (no `TG-210` formula exists to conform to either way) and have Population Ecology instead reuse `AgricultureSystem`'s `Food` output and `ReproductionSystem`'s existing `foodPerCapita >= 3.0`/`HasAvailableHousing` gates as its carrying-capacity inputs — the same "reuse an existing field" posture every RFC since RFC-004 has used. **A real gap was found while writing the ADR**: `AgricultureSystem` had zero dedicated unit tests since early in this project, only ever exercised indirectly through `SurvivalSimulationTests`'s multi-year integration runs — fixed with 9 new tests (`AgricultureSystemTests.cs`) covering the season modifiers, the moisture penalty, seed consumption, and the `FarmHarvestedEvent` payload. `RFC/RFC-008-population-ecology-carrying-capacity.md` written, scoping Week 14 — `Settlement.CarryingCapacity = Math.Min(HousingCapacity, Food/3.0)` plus `PopulationDeclineEvent`/`PopulationBoomEvent` (2 of `TG-240`'s 10 named events), detection only. Its proposed formula was sanity-checked against a resumed live simulation's real settlement data before implementation began. Backlog table's "Life Sciences foundation" row narrowed to reflect Population Ecology now being unblocked and scheduled. |
| 2026-07-13 | **Week 14 (Population Ecology: Carrying Capacity, Increment 2 of Volume IV) shipped and tested:** `Settlement.CarryingCapacity` (new EF migration `AddSettlementCarryingCapacity`, applied live) + `PopulationEcologySystem` (monthly cadence) computes carrying capacity from existing `Food`/housing inputs; a settlement whose population exceeds its capacity publishes `PopulationDeclineEvent`; real population growth while comfortably under capacity publishes `PopulationBoomEvent`. Also added `Settlement.HousingCapacity` as a real `int` property (previously only exposed as the boolean `HasAvailableHousing`), refactoring the latter to reuse it. **Both new events were subscribed to `HistorySystem` at introduction time**, continuing the practice reinforced Week 12 Day 61. **A real design flaw was found and fixed during implementation, before it ever shipped**: the first `PopulationBoomEvent` draft used a pure pressure-crossing model (mirroring `TerritorySystem`'s expand/contract logic), but writing the unit tests exposed that this can never fire — real population growth *raises* pressure, it never lowers it, so a "pressure fell below X" trigger can never coincide with the population actually growing. Redesigned around a state transition (`isGrowingComfortably`, tracked via its own dictionary) so the event fires once when that state begins rather than never firing at all. 6 new `PopulationEcologySystemTests` plus 2 more for the `HistorySystem` wiring (175 total, up from 158, including the Week 13 Day 63 `AgricultureSystem` tests). Minimal Observatory surfacing added: a settlement's "Population" section (carrying capacity progress bar). Verified live against a resumed simulation across all 8 real settlements — `CarryingCapacity` computed correctly from real data (most `0` because their `Food` storage is genuinely `0` right now, the same scarcity Week 12 Day 59 independently observed — the mathematically correct output, not a bug; one settlement had real food and showed a real non-zero capacity). Observatory rendered correctly in both the real-number and zero-capacity cases with no console errors. No `PopulationDecline`/`PopulationBoomEvent` occurred organically within the verification window — consistent with the Week 11 Day 54 / Week 12 Day 59 precedent of not fabricating false positives; both mechanisms are directly unit-tested, including the specific false-positive scenario RFC-008's own open questions worried about. Full solution build clean, 175/175 unit tests, 3/3 fast integration tests, `tsc --noEmit` clean. `RFC-008` marked Implemented. |
| 2026-07-13 | **Week 15 (Disease & Health: Overcrowding-Driven Infection, Increment 3 of Volume IV) shipped and tested:** `RFC/RFC-009-disease-health-overcrowding.md` applies `TG-260`'s disease concept to `Citizen` (the one population that exists), reusing Week 14's `CarryingCapacity`/`Population` overcrowding signal directly. A new pure in-memory `Infection` entity (following the `LegalCase`/`Apprenticeship` pattern, no EF migration) + `DiseaseSystem` (daily cadence): citizens in overcrowded settlements risk infection; severity damages the existing `Needs.Health`; severity reaching its max kills the citizen via the *existing* `CitizenDiedEvent` (cause `"Disease"`) rather than a parallel death path; recovery chance scales with current health. Settlement-level infection-rate crossings publish `EpidemicStarted`/`ContainedEvent`. All 4 events (`OrganismInfected`, `DiseaseRecovered`, `EpidemicStarted`, `EpidemicContained`) subscribed to `HistorySystem` at introduction time. **A real archiving bug was caught by the regression tests, not live verification**: `OrganismInfected`/`DiseaseRecovered` were first archived at severity `4.0`, silently classified "Low" (not archived) by `SignificanceEvaluator` since it requires `> 4.0` for "Medium" — bumped to `5.0`. 7 new `DiseaseSystemTests` plus 4 more for the `HistorySystem` wiring. Minimal Observatory surfacing added: a settlement's "Health" section (active infection count). Verified live against a resumed simulation run to Year 2: 514 `OrganismInfected`, 436 `DiseaseRecovered`, and 39 `EpidemicStarted` records fired organically; two real settlements showed live active infections in the UI with no console errors. |
| 2026-07-13 | **Week 16 (Evolution & Adaptation: Adaptive Drift Detection, Increment 4 of Volume IV) shipped and tested:** `RFC/RFC-010-evolution-adaptive-drift.md` observes the population-level attribute drift that `ReproductionSystem`'s inheritance-with-variance and `CitizenSystem`'s differential survival already produce — "Individuals do not evolve. Populations evolve over many generations," per `TG-250` itself — rather than adding a second selection mechanic. `EvolutionSystem` (yearly cadence) compares each settlement's average `Citizen.Attributes` against the prior year's; a meaningful shift publishes `AdaptiveShiftObservedEvent`, and 3 consecutive stagnant years publish `EvolutionaryStagnationEvent`. No new `Settlement`/`Citizen` field added — averages tracked only in the system's own in-memory dictionaries. Both events subscribed to `HistorySystem` at introduction time. **A subtle correctness bug was caught before it ever shipped**: the first draft counted a settlement's very first yearly evaluation (no baseline to compare against) as "stagnant," which could have fired `EvolutionaryStagnationEvent` after only 3 calls on a brand-new settlement rather than 3 real years of genuine stagnation — fixed by skipping the stagnation counter entirely on the baseline-establishing year, the same "no event on the first `Execute()`" convention `TerritorySystem`/`PopulationEcologySystem` already use, applied correctly to a secondary counter this time. 5 new `EvolutionSystemTests` plus 2 more for the `HistorySystem` wiring (193 total, up from 175). Minimal Observatory surfacing added: a settlement's "Adaptation" section (live Attribute averages). Verified live against the same resumed simulation run: 37 organic `AdaptiveShiftObserved` records by Year 2, real diverging Attribute averages visible per settlement (e.g. Deepmill: Strength 3.7 vs. Littledale: Strength 4.09, Perception 7.71 vs. 3.1), no console errors. Full solution build clean, 193/193 unit tests, 3/3 fast integration tests, `tsc --noEmit` clean. Both `RFC-009` and `RFC-010` marked Implemented. Backlog table's "Life Sciences foundation" row narrowed to just Fauna/Decomposers, the only remaining items that genuinely need a new wildlife/microbe species concept rather than reusing `Citizen`. |
| 2026-07-13 | **Week 17 (Decomposers & Microbiology: Soil Health, Increment 5 of Volume IV) shipped and tested:** `RFC/RFC-011-decomposers-soil-health.md` — the first RFC in this series to write into an earlier system's formula rather than staying strictly read-only, since `TG-220` explicitly names Agriculture as directly influenced by decomposition. `Settlement.SoilHealth` (new EF migration `AddSettlementSoilHealthAndWildlife`, applied live, default `100.0`) + `DecomposerSystem` (monthly cadence) subscribes to the existing `CitizenDied`/`ForestDeclined` events (organic matter in) and the existing `FarmHarvestedEvent` (soil depletion out); `AgricultureSystem.ProcessFarm` now multiplies its yield by `SoilHealth / 100.0` — a no-op at the default, confirmed not to change any existing `AgricultureSystemTests` assertion. 3 events (`NutrientPulseOccurred`, `OrganicMatterAccumulated`, `WasteFullyDecomposed`) subscribed to `HistorySystem` at introduction time. **Two real bugs were caught before they ever reached the live database or shipped**: (1) `dotnet ef migrations add` scaffolded `defaultValue: 0.0` for the new `SoilHealth` column despite the C# property defaulting to `100.0` (EF scaffolds the CLR zero-value, not the property initializer) — applying it as-generated would have silently reset every existing settlement's soil to "fully depleted"; caught by reviewing the migration file before running `database update`, fixed to `defaultValue: 100.0`. (2) The first `Execute()` draft captured the "previous" `SoilHealth` baseline *after* that same evaluation's own decomposition had already updated it, making a real first-ever rise undetectable — caught by the regression tests, fixed by capturing the baseline before decomposition. 6 new `DecomposerSystemTests` plus 3 more for the `HistorySystem` wiring. Minimal Observatory surfacing added: a settlement's "Ecology" section (Soil Health progress bar). Verified live: all 8 real settlements correctly retained `SoilHealth = 100` after the corrected migration. |
| 2026-07-13 | **Week 18 (Fauna & Animal Behavior: Aggregate Wildlife Population, Increment 6 of Volume IV) shipped and tested:** `RFC/RFC-012-fauna-aggregate-wildlife.md` — the first RFC in the Life Sciences series that cannot reuse `Citizen`, since no wildlife population has ever existed in this codebase. Scoped strictly to `TG-230`'s own Performance Considerations sentence ("Most animal populations should be simulated using aggregate ecological models"), not its individual-animal prose. `Settlement.WildlifePopulation` (shared EF migration with Week 17, default `0.0`) + `FaunaSystem` (monthly cadence) computes habitat capacity from real `Forest`-terrain tile count within the settlement's territory (reusing `EcologySystem`'s existing forest-tracking signal, no new environmental state) and moves the population toward it each month. `SpeciesExpandedEvent` fires on meaningful growth while under capacity; `AnimalDiedEvent` fires on a meaningful die-off, reinterpreted at the aggregate level (documented in `RFC-012`) rather than a single animal's literal death. Both subscribed to `HistorySystem` at introduction time. 4 new `FaunaSystemTests` plus 2 more for the `HistorySystem` wiring (208 total, up from 193). Minimal Observatory surfacing added, sharing Week 17's "Ecology" section (Wildlife number). Verified live against a resumed simulation run to Year 1: `WildlifePopulation` diverged meaningfully across all 8 real settlements by real forest cover (0 to 56), 37 organic `SpeciesExpandedEvent` records archived, no console errors. Full solution build clean, 208/208 unit tests, 3/3 fast integration tests, `tsc --noEmit` clean. Both `RFC-011` and `RFC-012` marked Implemented. **Volume IV (Biological Sciences) now has a shipped first increment for all six of its foundational documents** (Flora, Population Ecology, Disease & Health, Evolution & Adaptation, Decomposers & Microbiology, Fauna & Animal Behavior) — the Backlog table's "Life Sciences foundation" row is retired. |

---

## Quick Links

- Philosophy start: [`01_Foundations/TG-000_Vision.md`](01_Foundations/TG-000_Vision.md)
- Non-negotiable rules: [`01_Foundations/TG-001_Constitution.md`](01_Foundations/TG-001_Constitution.md)
- System architecture: [`02_Engine/TG-002_Software_Architecture.md`](02_Engine/TG-002_Software_Architecture.md)
- Repo/project layout: [`02_Engine/TG-003_Project_Structure.md`](02_Engine/TG-003_Project_Structure.md)
- Engineering standard (PR/lint checklist): [`06_Development/TG-DEV-000_Engineering_Standard.md`](06_Development/TG-DEV-000_Engineering_Standard.md)
- Latest implementation status: [`06_Development/TG-DEV-009_AI_Production_Readiness.md`](06_Development/TG-DEV-009_AI_Production_Readiness.md)
- Execution plan: [`DEVELOPMENT_PLAN.md`](DEVELOPMENT_PLAN.md)
- Active ADRs: [`ADR/ADR-001-empty-project-disposition.md`](ADR/ADR-001-empty-project-disposition.md)
- Active RFCs: [`RFC/RFC-001-emotion-system.md`](RFC/RFC-001-emotion-system.md) (Week 3 scoping)

---

## Architecture Overview

Six layers, strictly downward-dependent (per `TG-002`/`TG-003`):

```
World Observatory (React)        — read-only presentation, never mutates state
        ↓
Garden API (ASP.NET Core)        — 17 controllers, 5 SignalR hubs, read/observe + admin only
        ↓
Simulation Engine                — the only layer allowed to mutate World State
        ↓
World State Repository           — "the present," mutable, validated every tick
        ↓
Historical Archive                — "the past," append-only, immutable
        ↓
Persistence (PostgreSQL via EF Core)
```

### Documented project layout (`TG-003`) vs. what's actually built

| Project | Spec role | Actual state |
|---|---|---|
| `Garden.Core` | Time, IDs, event/interface contracts | ✅ Implemented |
| `Garden.World` | Entities, world-state collections | ✅ Implemented (17 entity types) |
| `Garden.Engine` | All simulation disciplines, rules, systems | ✅ Implemented (12 systems, 15 services) — carries logic TG-003 assigned elsewhere (see below) |
| `Garden.Infrastructure` | EF Core, persistence, migrations | ✅ Implemented |
| `Garden.Api` | Controllers, SignalR hubs | ✅ Implemented (17 controllers, 5 hubs) |
| `Garden.Observatory` | React/Vite frontend | ✅ Implemented (9 pages, builds clean) |
| `Garden.Contracts` | DTOs / event contracts (per `TG-DEV-002`) | ⚠️ Empty `.csproj`, referenced by the build graph but unused — role absorbed into `Garden.Core` |
| `Garden.Shared` | Math/random/collection utilities (per `TG-DEV-002`) | ⚠️ Empty `.csproj`, referenced by the build graph but unused — e.g. `SimulationRandom.cs` lives in `Garden.Engine/Random/` instead |
| `Garden.Story` | Story engine, narrative templates | ⚠️ Empty `.csproj`, **not referenced anywhere** — `StoryEngine.cs` actually lives in `Garden.Engine/Services/` |
| `Garden.Simulation` | (role never concretely assigned) | ⚠️ Empty `.csproj`, **not referenced anywhere**, no orphaned logic exists for it either |
| `Garden.Tools` | (role never concretely assigned) | ⚠️ Empty `.csproj`, **not referenced anywhere** |

This drift (5 of 11 declared projects are hollow, with logic consolidated into `Garden.Engine`
instead) is a real spec-vs-code divergence, not just unstarted work — see `DEVELOPMENT_PLAN.md`
Day 3 for the proposed resolution (an ADR, not a rewrite).

### Coverage snapshot by science volume (detail in `DEVELOPMENT_PLAN.md`)

| Volume | Folder | Implementation reality |
|---|---|---|
| III — Natural | `03_Sciences/01_Natural` | ~40% reflected (Weather/Season/Hydrology/Resource/Ecology systems exist but are simplified placeholders relative to spec scope) |
| IV — Life | `03_Sciences/02_Life` | ~0% — no Flora/Fauna/Disease/Evolution/Domestication/Extinction system exists |
| V — Cognitive | `03_Sciences/03_Cognitive` | Thin subset only (Needs = physiological only; Personality = 6 of 16+ dimensions); **Emotion and Perception have zero code footprint** |
| VI — Social | `03_Sciences/04_Social` | Economics/Culture/Religion partially real; Politics is a 4-item population lookup table; Communication/Language/Groups(generic)/Norms/Education/Law = 0% |
| VII — Civilization | `03_Sciences/05_Civilization` | Diplomacy/Kingdoms/Trade reasonably implemented; **Warfare and Borders/Territory have zero code footprint**; Technology's implementation directly contradicts its own spec's "no predefined tech tree" principle |
| — Story | `04_Story` | Core services exist (`StoryEngine`, `HistoryManager`, `SignificanceEvaluator`, `MemoryService`, `TimelineService`) but Legends/Myths generation is unimplemented |
| — Observatory | `05_Observatory` | Dashboard/map/search/notifications implemented; multi-scale camera, layered maps, replay/branching, accessibility, and modding are all unimplemented |

---

*This index is a living document. Update it whenever a specification is added, renamed,
retired to `Archive/`, or promoted from `RFC/`. Record the reasoning behind non-obvious
structural decisions as an ADR rather than editing history in this Change Log.*
